<?xml version="1.0" encoding="utf-8" ?>
<configuration>
  <!--
    SECURITY NOTE: To avoid storing credentials here, set the environment variable:
      WEEKLY_REPORT_CONNECTION_STRING=<your connection string>
    The application checks that variable first before reading from this file.
  -->
  <connectionStrings>
    <add name="DefaultConnection"
         connectionString="Server=KAVALA-L\SQLEXPRESS,1433;Database=WeeklyReport;User Id=weekly_report_user;Password=Wr@2026StrongPass!;Encrypt=True;TrustServerCertificate=True;" />
  </connectionStrings>
  <appSettings>
    <add key="Port" value="4443" />
    <!--
      AllowedOrigins: comma-separated list of allowed CORS origins.
      Leave empty to allow all origins (development only).
      Example: http://localhost:3000,https://myapp.example.com
    -->
    <add key="AllowedOrigins" value="http://localhost:3000,http://localhost" />
  </appSettings>
</configuration>
